# sdk-react

Channel: alpha

Features:
- Provider + hooks wrapping core SDK lifecycle
- SSR-safe initialization (no window access at render)
- Theming token pass-through (accentColor, borderRadiusPx)
