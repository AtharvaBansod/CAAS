// Fastify's type-provider-zod handles most of this automatically
// This file can export the provider setup if needed
export { serializerCompiler, validatorCompiler } from 'fastify-type-provider-zod';
